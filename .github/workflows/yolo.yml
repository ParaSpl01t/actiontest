name: GitHub Actions Demo
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: touch hello.txt
      - run: |
          ls ${{ github.workspace }}
      - run: ls -lah
